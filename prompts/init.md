This is a prompt for initializing this project.

# Goals

The goal of this project is to create a Playwright test automation runner for `tape-six`. It should be an exact clone of `tape-six-puppeteer` but using Playwright instead of Puppeteer.

You can access all related projects locally:

- `tape-six` &mdash; ../tape-six/
- `tape-six-puppeteer` &mdash; ../tape-six-puppeteer/
- `tape-six-proc` &mdash; ../tape-six-proc/

The working code that runs tests using Playwright can be found in ../tape-six/tests/playwright-chrome.js

## Technical details

All technical aspects should be identical to `tape-six-puppeteer`. Should be created:

- NPM-specific files.
- GitHub-specific files.
- Git-specific files.
- Documentation files including README.md and wiki.
- All AI-related files.
- `.windsurf` directory with workflows.
- `workflows` directory with workflow files.

Try to clone as much as you can adapting accordingly.
